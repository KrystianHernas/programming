#include "judge.h"
#include <algorithm>
#include <cstring>

class PIE : public IPIE {
public:
  int getMaxPierniczkiCount(int count, int favorite[10][4], int available[4]) {
    int res = -1;
    return res;
  }
};

int main() {
  PIE solution;
  Judge::run(&solution);
  return 0;
}